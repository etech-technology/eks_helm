apiVersion: v2
name: pyspa
description: Python single-page app (FastAPI) + Postgres (Bitnami) using Helm
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: postgresql
    version: 18.2.4
    repository: https://charts.bitnami.com/bitnami
